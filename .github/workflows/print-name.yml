name: print-name

on:
  push:
    branches:
      - main

jobs:
  say-my-name:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Print your name
        run: echo "Hello, my name is Rituraj!"
